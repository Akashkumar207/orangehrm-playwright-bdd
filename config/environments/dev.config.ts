/**
 * OrangeHRM only publishes one public demo instance, so dev/qa/prod all
 * point at the same URL here. In a real project with separately deployed
 * environments, each of these files would hold that environment's own
 * baseURL (and any other environment-specific values).
 */
export default {
  baseURL: 'https://opensource-demo.orangehrmlive.com',
};
